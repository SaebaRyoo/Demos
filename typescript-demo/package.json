{
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "build": "ts-node index.ts",
    "watch": "ts-node-dev --respawn --transpile-only index.ts",
    "build:m": "node_modules/.bin/ts-node index.ts"
  }
}
